language: php

php:
  - 7.1
  - 7.2

env:  
  - LARAVEL_VERSION=5.5.*

matrix:
  fast_finish: true

before_script:
- cp .env.travis .env
  - composer self-update
  - composer install --no-interaction
  
script:
  - phpunit
  
notifications:
email:
    recipients:
        - mohammedosama@sectheater.org
    on_success: always
    on_failure: always
